{
  "name": "local-dependency-resolver",
  "description": "Resolves Maven dependencies (groupId/artifactId) to local source checkouts under the user's configured projects root (~/projects by default, overridable via LOCAL_DEPENDENCY_RESOLVER_ROOT) so Claude can read and edit dependency code directly instead of inspecting JARs.",
  "version": "1.0.0",
  "author": {
    "name": "Lars Nielsen"
  }
}
